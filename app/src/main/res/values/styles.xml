<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material">
        <!-- Customize your theme here. -->
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="android:ThemeOverlay.Material.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="android:Theme.Material.Light" />
    <style name="blueActionBar">
        <item name="android:background">#33a0ff</item>
        <item name="android:textColor">#ffffff</item>
    </style>

</resources>
